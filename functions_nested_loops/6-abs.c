#include "main.h"
/**
 * _abs - prints absolute value of an integer.
 *@n: to prove
 * Return: Return the absolute value of an integer.
 */
int _abs(int n)
{
	if (n < 0)
		return (n * -1);
	else
		return (n);
}
