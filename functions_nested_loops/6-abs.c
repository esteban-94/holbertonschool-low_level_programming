#include "main.h"
/**
 * _abs - prints absolute value of an integer.
 * Return: Return 1 if is alphabetic character or 0 otherwise.
 */
int _abs(int n)
{
	if (n < 0)
		return (n * -1);
	else
		return (n);
}